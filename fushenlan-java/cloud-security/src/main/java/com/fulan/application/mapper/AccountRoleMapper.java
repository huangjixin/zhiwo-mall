package com.fulan.application.mapper;import java.util.List;import java.util.Set;import org.apache.ibatis.annotations.Param;import org.apache.ibatis.annotations.Select;import org.springframework.stereotype.Repository;import com.baomidou.mybatisplus.mapper.BaseMapper;import com.fulan.api.security.domain.AccountRole;@Repositorypublic interface AccountRoleMapper extends BaseMapper<AccountRole>{		int deleteAccountId(Long accountId);    	int  inserBylist( List<AccountRole> accountRoles);	List<Long> selectRoles(Long accountId);		List<String> selectAccountRoleManageVo(Long accountId);		List<String> selectAccountRoleManageMinVo(Long accountId);		List<String> selectByAccountId(Long accountId);	/**	 * 根据accountId 查询roleId	 * @param accountId	 * @return	 */	List<Long> seleByAccountIdList(@Param("accountId")Long accountId);}